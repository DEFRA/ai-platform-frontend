{% extends 'layouts/page.njk' %}

{% from "govuk/components/warning-text/macro.njk" import govukWarningText %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}

{% block content %}
  {{ appHeading({
    text: heading
  }) }}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      {% if errorSummary %}
        {{ govukErrorSummary({
          titleText: "There is a problem",
          errorList: errorSummary
        }) }}
      {% endif %}

      {{ govukWarningText({
        text: "Revoking " + model.displayName + " cannot be undone. You will need to connect again to get a new credential.",
        iconFallbackText: "Warning"
      }) }}

      <form method="post" novalidate>
        <input type="hidden" name="crumb" value="{{ crumb }}">

        {{ govukRadios({
          idPrefix: "confirmRevoke",
          name: "confirmRevoke",
          fieldset: {
            legend: {
              text: "Are you sure you want to revoke this credential?"
            }
          },
          errorMessage: fieldErrors.confirmRevoke,
          items: [
            { value: "yes", text: "Yes, revoke this credential" },
            { value: "no", text: "No, take me back to Manage AI access" }
          ]
        }) }}

        {{ govukButton({ text: "Continue" }) }}
      </form>
    </div>
  </div>
{% endblock %}
